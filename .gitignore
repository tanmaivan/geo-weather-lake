# --- General Python ---
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
venv/
pip-log.txt
pip-delete-this-directory.txt

# --- Environment Variables & Secrets (CRITICAL) ---
.env
.env.*
!.env.example

# --- IDE & OS Metadata ---
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# --- Airflow ---
# Ignore logs generated by Airflow
airflow/logs/
airflow/*.pid
airflow/*.err
airflow/*.log
airflow/config/airflow.cfg
# Ignore standalone sqlite db if generated locally
airflow/airflow.db
airflow/airflow.db-journal

# --- dbt (Data Build Tool) ---
# Compiled SQL and build artifacts
dbt/**/target/
# Installed packages/dependencies
dbt/**/dbt_packages/
# dbt logs
dbt/**/logs/
dbt/**/dbt_modules/

# --- DuckDB & Database Files ---
# We do NOT want to commit the actual database file containing data
*.duckdb
*.duckdb.wal
*.duckdb.tmp
# Specifically for your structure
dbt/weather_analytics/dbt.duckdb
dbt/weather_analytics/dbt_build.duckdb
dbt/weather_analytics/dbt_tmp.duckdb

# --- Jupyter Notebooks ---
.ipynb_checkpoints/
*/.ipynb_checkpoints/

# --- Docker ---
# Ignore local volume data directories if they are created in the root
minio-data/
postgres-data/
metabase-data/

# --- Pre-commit ---
.pre-commit-config.yaml.save

# --- Explicit Exceptions (Files we WANT to keep) ---
# We want to keep the extensions .gz files for offline build support
!airflow/extensions/*.gz
